<nav class="w-full px-6 md:px-12 py-4 flex items-center justify-between">
  <div class="flex items-center space-x-3">
    <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-neon-pink via-neon-purple to-neon-cyan flex items-center justify-center text-black font-bold">
      T
    </div>
    <div>
      <a href="<%= root_path %>" class="text-white font-semibold text-lg">Toodel</a>
      <div class="text-xs text-gray-400">Find teammates. Finish projects.</div>
    </div>
  </div>

  <div class="flex items-center space-x-4">
    <%= link_to "Discover", discover_path, class: "hidden md:inline-block text-gray-300 hover:text-white" %>
    <%= link_to "How it works", "#how", class: "hidden md:inline-block text-gray-300 hover:text-white" %>

    <% if user_signed_in? %>
      <div class="flex items-center space-x-3">
        <div class="text-sm text-gray-300">Hi, <strong class="text-white"><%= current_user.name || current_user.email.split('@').first %></strong></div>
        <%= link_to "Dashboard", dashboard_path, class: "px-3 py-1 bg-white/5 rounded-md hover:bg-white/10" %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "ml-2 px-3 py-1 bg-gradient-to-r from-neon-pink to-neon-cyan text-black rounded-md" %>
      </div>
    <% else %>
      <%= link_to "Log in", new_user_session_path, class: "text-gray-300 hover:text-white" %>
      <%= link_to "Sign up", new_user_registration_path, class: "ml-2 px-3 py-1 bg-gradient-to-r from-neon-pink to-neon-cyan text-black rounded-md" %>
    <% end %>
  </div>
</nav>
