/*
  channels/index.js
  Explicitly import ActionCable channels for Importmap (no globEager).
  Add a new `import "./your_channel_name"` line here when you create a new channel.
*/

import "./consumer"
import "./mutual_matches_channel"
